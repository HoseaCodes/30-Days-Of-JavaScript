// String Data Type
const name = 'Dominique'

// Boolean Data Type
const isOn = True;

// Undefined Data Type
const home

// Null Data Type
const none = null;

// Typeof operator
console.log(typeof name)
console.log(typeof isOn)
console.log(typeof home)
console.log(typeof none)

// Declare four variable without assigning values
const one
const two
const three
const four

// Declare four variables with assigned values
const toy = bear;
const car = BMW;
const tv = Samsung;
const shirt = BR;

// Declare variables to store your first name, last name, martial status, country and age in multiple lines
const firstName = 'Dominique';
const lastName = 'Hosea';
const isMarried = False;
const country = 'US';
const age = 28;

// Declare variables to store your first name, last name, marital status, country and age in a single line
let fName = 'Dominique',
    lName = 'Hosea',
    isSingle = True,
    country = 'US',
    age = 28;

// Delcare two variables myAge and yourAge and assign them initial values and log to the browser console.

const myAge = 28;
const yourAge = 30;

console.log(`My age ` + myAge, `Your age ` + yourAge)